image: php:8.1

stages:
    - test
    - deploy

test:
    stage: test
    tags:
        - build
    script:
        - echo "hello world"
    
deploy:
    stage: deploy
    tags:
        - build
    script:
        - echo "deploying"
        